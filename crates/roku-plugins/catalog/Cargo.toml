[package]
name = "roku-plugin-catalog"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
roku-common-types = { path = "../../roku-common-types" }
serde = { version = "1", features = ["derive"] }
